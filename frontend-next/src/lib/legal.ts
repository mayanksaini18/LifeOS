// Both legal documents reference these, so a change lands in one place.

// TODO: swap for a domain address (privacy@…) once one exists. A personal
// Gmail on a public page gets scraped, and DPDP grievance requests are
// supposed to reach a contact tied to the service.
export const LEGAL_CONTACT_EMAIL = "sayzoindia@gmail.com";

// Bump only when the text changes materially — the date is what tells a
// returning reader whether they need to re-read the document.
export const LEGAL_LAST_UPDATED = "14 August 2026";
